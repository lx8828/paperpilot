# PaperPilot 开发记录

> 本文档记录 PaperPilot（LLM 论文深度精读系统）开发过程中**遇到的问题、做的决策、踩的坑**。
> 目的：① 面试时能讲清楚"我们为什么这么设计"；② 几个月后的自己回来改代码时，能快速想起每条规则背后的原因，避免重蹈覆辙。

---

## 0. 一句话定位

输入一篇 PDF 论文 → 系统自动完成：解析 → claims 提取 → **证据溯源验证** → 语义去重 → 角色打标 → 重要性打分 → 论证骨架 → 图表指南 → 完整精读报告（导读/概述/骨架/章节精读）。

**核心信条：所有结论必须能回到原文（溯源闭环）。** 任何一步产出如果不能指向原文页码，就视为不合格——这是用来对抗 LLM 在学术场景下的幻觉。

**给谁用（2026-09-07 定）**：被英文/专业门槛劝退的普通人。价值 = 讲得懂（帮读）+ 不编造（拒答）+ 可溯源（闭环）；"帮高手精确摘原文细节"明确不是目标（见阶段 P）。

---

## 1. 处理流程与产物

```
src/paperpilot/storage/papers/*.pdf
   │  run_claims.py    parse → chunk → LLM 提取 claims + evidence 验证
   │  run_scan_noise.py  噪声扫描（可选质检）
   ▼
out_claims/<pdf>.claims.json      claims + 溯源锚点（chunk_id/title_path/page）
   │  run_view.py     LLM 语义去重 → LLM 角色打标 → 规则算分
   ▼
out_views/<pdf>.summary.json      主张组（label/importance/score/ev_state）
   │  run_skeleton.py  必读 Hub 建 implements/supports/limits/contrasts 关系边
   ▼
out_views/<pdf>.skeleton.json     论证骨架
   │  run_figures.py / run_report.py
   ▼
out_views/<pdf>.report.md         最终报告
   │  run_bench.py  ← 回归护栏：扫全部论文，模块勾叉 + 基线 diff
   ▼
bench/baseline.json               21 篇基线快照
```

核心模块在 `src/paperpilot/tools/`：`pdf_parser → heading → chunker → analyzer → evidence → viewer → skeleton → figures → report`。

---

## 2. 演进时间线：遇到的问题与解决

### 阶段 A：claims 提取（"论文理解"是一切的起点）

**背景**：最初设计文档画了很大蓝图（LangGraph、向量检索、图谱……）。但讨论后定下原则：**先别搭后面，先证明"论文理解"本身可行**。理解做不出来，检索/问答都是空转。

**设计**：对每个正文 chunk 调 LLM，提取四类 claims：`contribution / method / result / limitation`。每条 claim 必须带 `evidence_quote`（原文逐字支持片段）→ 为溯源闭环做准备。

**踩坑 1 — 表格数据行会拼成伪句子**：
PDF 表格被解析成一行一个值（`58.2%`、`0.11`），直接喂给 LLM 会被拼成"我 58.2% 认为…"这种假 claim。
**解决**：提取前用正则把纯数值/百分比/序号短行替换成 `[TABLE_CELL]` 占位符，模型不会据此生成 claims。
**代价（后来意识到）**：表格里的数字因此永远进不了 claims。所以"图表信息必须被正文提到"成为系统适用性的硬条件——正文没提的关键数字会静默丢失。

### 阶段 B：证据溯源验证（Verifier）

**问题**：LLM 的 evidence_quote 抄自 PDF 原文，但原文经 pymupdf 提取后常有：断行连字符（`informa- tion`）、弯引号/破折号、数学符号字形、大小写差异——**逐字比对永远失败**。
**解决**（`evidence.py`）：
- `norm_text`：大小写折叠 + 弯引号转 ASCII + 空白折叠
- `alnum_norm`：剥离所有非字母数字，吸收断行/字形噪声
- 三档判定：`hit`（归一后是原文子串）/ `loose`（省略式摘录 `…` 各段都在）/ `miss`（对不回）
- miss 再查"是否漂移到别的 chunk"，区分"验证器太严"还是"真漂移"

**教训**：PDF 文本提取的噪声是分层的，单层归一化不够，需要"逐字 → 字形级 → 分段"三级容错。

### 阶段 C：去重——从"规则没用"到"必须语义级"

**问题**：一篇论文能提 100~180 条 claims，同一主张在 Abstract/Intro/正文反复出现，用户看到一片重复，找不到重点。

**尝试 1（失败，数据说话）**：规则级去重——evidence 完全重复 / 词 Jaccard≥0.9 / 字符 bigram≥0.7。跑完全部 1934 条：**只并掉 2 条**。
**洞察**：claims 的"重复"是**语义级**的，不是字面级：
1. `text` 是 LLM 每次现场概括的中文句，措辞必不同
2. `evidence_quote` 来自不同 section 的不同原文句（作者本来就用不同的话重述同一结论）
→ 任何基于字符/词的规则在这个数据上都抓不到东西。

**解决（路线 B：LLM 全局归并）**：把整篇 claims 一次性喂给 LLM，输出"同义组"。**关键设计**：
- 不让 LLM 自由发挥"合并成新句"——那会引入无法溯源的新文本
- 强制 **rep 必须从组内成员里挑**（信息最完整的那条原句），其余成员降级为 occurrences
- 副产品：`claim_ids` 数量 = 该主张被论文重复强调的次数 → 天然是打分的权重信号

### 阶段 D：重要性打分——"LLM 只分类，分数我们用公式算"

**问题**：报告还是太多条，用户要的是"这篇论文先看这 3 条"。给句子打 1~5 分，直接让 LLM 打会不稳定、不可解释、权重不可调。

**设计**：LLM 只把每个组归到 **8 类角色 label**；真正的分数是后端规则公式算的（可解释、可调参）：
`core_claim / result_primary / result_supporting / method_core / ablation / detail / limitation / related`

**踩坑 2 — label 命名**：早期想把主要结果叫 `finding` 或 `key_result`。统一成 `result_primary / result_supporting` 前缀——**杜绝"finding 和 key_result 是不是一回事"的歧义**，下游模型不会分不清。

**打分规则的演进（吸收用户 3 点意见）**：
1. **位置加权激进分档**（不是 0.3 小步）：ABSTRACT +1.0 / Conclusion+Discussion +0.8 / Introduction +0.6 / 正文 +0 / 附录 −0.5。"出现在哪个 section 本身就是强信号"。
2. **数字不是见数就加**：只有 `result_*` 类才算"量化强度"；`detail` 里的"6 个随机种子"不加分——数字只有作为结果证据才值钱。
3. **evidence 状态参与加减**：`hit/loose` 加分、`miss` 减分并打 `⚠待核` 标——没有原文证据支撑的组不能拿高分。

**分层输出**：必读 ≥5 / 应读 4 / 按需 <4，报告按此组织，用户先看 3 条核心。

### 阶段 E：冷启动与"论文适合度"（边界测试 00859）

**发现**：全新论文 00859（VRP 强化学习）全链路零报错。抽查结论节发现 limitation 只有 2 条——**不是漏提，是那篇论文根本没有 Discussion/Limitations 章节**，唯一的局限声明就是结论一句 Future work。

**沉淀"系统适配范围"**（凭解析机制逐条验证）：
适合 = 有可选中的文本层（非扫描件）+ 编号章节规整 + 页眉页脚简单 + 公式少 + 图表少且信息正文提到 + 篇幅适中。
**一句话速筛**：全文能选中复制不乱码 → 抽查一页文本按人读顺序连贯 → 扫实验章标题格式统一。三关过了就能用；不过的话后面环节会成批出错。

### 阶段 F：Bench 回归基准（最有工程价值的一个）

**用户原话精神**："改了代码跑一遍，看勾叉变化，知道哪儿改出问题了，就行。"——**这是回归护栏，不是验收测试**。

**设计**（`run_bench.py`）：
- 10 个模块：`PARS 解析 / HDR 标题分块 / CLA claims提取 / EVID 证据溯源 / NS 噪声 / DEUP 去重 / LAB 打标打分 / SKEL 骨架 / FIG 图表 / RPT 报告`
- 三类判定：本地重算（重新 parse+chunk+evidence，**不调 LLM**，检测解析层改动）/ 产物检查（只读 JSON）/ 可跳过（NS 无 noise.json 时 `·`）
- 每次跑自动存快照 `bench/snapshots/<ts>.json`；`--save-baseline` 固化基线；自动 diff 输出"回归(勾→叉) / 改善(叉→勾)"
- **好论文的叉 = 改出回归；恶意论文的叉变勾 = 系统变强了**

**踩坑 3 — 模块间耦合**：一开始某模块 fail 就提前 return，导致后面模块被跳成 `·`（EVID 信息丢失）。改为**各模块独立判定**，一个失败不阻断其它检测。

### 阶段 G：恶意论文测试（5 篇）——反直觉的收获

| 论文 | 预期雷点 | 实测 |
|---|---|---|
| 01456 | 奇偶页眉 | **意外干净**（10/10 或近全勾）|
| 01316 | 双栏 | 干净 |
| 01505/01555 | 数学论文 | **EVID 失守**（miss 38% / 29%）——数学符号被 PDF 打散，evidence 对不回原文 |
| 01599 | 双栏+公式+图表（AAS）| **全勾！最狠的反而不失守** |

**结论**：系统真正的能力边界**不是排版**（双栏/奇偶页眉都扛住了），而是**"符号/公式密集型论文"**（数学、定理证明），以及**"定理-证明"型论证结构**（31166：主定理 hub 找不到可建边的 claim，因为论证在证明过程里）。

**教训**：别凭直觉预测"什么会崩"，**跑数据**。盲猜会得出"01599 最烂"，实际它全绿。

### 阶段 H：幽灵标题（决定"不修"也是决策）

**现象**：正文句/符号碎片被误判成 L1 章节标题（31079 的 `3 TEACHER MODEL SYCOPHANTIC…`、01505 的 `2 Z2 ⊕Z3 Z2`、01599 的 `7 This approximation is valid…`）。

**三个成因**（都在 heading.py 的"信任分支"）：
1. **全大写无条件信任**：为 ICML/AAAI 全大写章节标题设计，但全大写正文句同样被放行
2. **`isupper()` 漏洞**：`'Z2 ⊕Z3 Z2'.isupper()` 返回 True（isupper 忽略数字符号）
3. **弱编号校验不认句子形态**：只查"首字母大写+长度"，不管是不是完整句

**决策：不修核心规则。** 理由（用户提的质疑被验证为正确）：
- 影响面是"章节归类元数据"：claims 内容、数字结论、evidence 溯源到**页码**都不依赖章节名 → 静态报告"乱一点"而非"读错"
- RAG 检索单元是 chunk，幽灵标题只是边界略歪、title_path 元数据不准，内容不丢、检索仍命中 → 不构成"不得不修"
- heading 是启发式打分系统，**每补一条规则都是对新版式开盲盒**，还可能误伤已验证论文 → 规则军备竞赛没有收敛点
- **处理**：bench 的 HDR 加"标题质量检测"（超长>70 字/含数学符号，占比≥25% 判叉），让缺陷如实可见但记录为"已知低危缺陷"，不进修复队列

**关键判别框架（后续可复用）**：一个问题值不值得修，看三件事——① 对静态产物是否误导/毁灭；② 对 RAG/问答是否必须；③ 修的成本是"一次决策"还是"无穷规则迭代"。幽灵标题三条都不占"必须"。

### 阶段 I：孤岛 Hub（这个值得修，但修法不同）

**现象**：骨架里某些必读 Hub（5 分核心主张）找不到任何关系边。

**性质分析（关键区分）**：孤岛**不是规则问题**，是 LLM 一次性建边的随机性/结构边界 → 没有"改 i 出问题"的军备竞赛，处理成本低。

**处理（两件事）**：
1. **无边自动重试**（最多 3 次）：验证结果——27843 的 `g1`（总主题句）从 0 边 → 11 边（**随机性孤岛被救回**）；31166 的 `g4/g12`（定理句）重试 3 次仍无边 → **确认是结构性孤岛**（定理的论证在证明里，正文没有可建边的 claim 组）
2. **孤岛标注**（`isolated_reason` 字段）：`no_sources`（论文缺该关系类型）/ `no_edges`（重试仍无边），报告按原因展示，不再错误地统一写成"论文总述句"

**副产品价值**：重试机制天然把"随机孤岛"和"结构孤岛"区分开，避免了把结构问题误当 bug。

### 阶段 J：Bench 判据校准（哨兵松紧）

- **SKEL 放宽**：孤岛占比 >25% 才判叉（1 个孤岛在 9 个 hub 里是常态噪音，不该报警）
- **HDR 调严**：加标题质量检测（抓真异常）
- **被校准抓出来的隐藏问题**：31079 原本显示全勾，但调严后暴露 3/7 幽灵标题——**"全勾"只代表没被查，不代表没问题**。这是哨兵存在的意义。

### 阶段 K：RAG 问答层（LangGraph）

**背景**：报告与前端闭环后，进入问答层。先做 QA 验证实验回答"PaperReport 是否真有用"，再落地检索。

**QA 验证实验（关键前置）**：对 12 道分三类的问题（R=报告顶层应能答 / C=需检索 claims / K=需原文），做分层上下文对照：
- R 类 4/4 仅靠 overview+core_points 答好 → 展示层真的有用
- C 类 5/6 顶层不足、给命中 claim 后补齐 → claim 层是不可或缺的第二层
- 结论：**结构化金字塔有效，PaperReport 没白做**（10/12 不需要 chunk）

**检索架构（三层漏斗）**：
- L1 展示层：overview 常驻（轻量，解决"全貌"类问题，无需检索）
- L2 Claim 检索：bge-m3 本地 embedding，检索单元是主张组（不是 chunk）
- L3 Chunk 升级：judge 判不足时，用命中 claim 的 `chunk_id` 拉原文（claim 提取时就锚定了，零成本索引）

**LangGraph 落地**（学习框架 + 目录实践）：
- 目录分层：`agents/`（state + nodes/，不依赖 langgraph，纯函数可单测）→ `graph/`（只接线）
- `QAState` 数据契约 → 四个节点 `retrieve_claims → judge_sufficiency →(条件边)→ pull_chunks / answer`
- 条件边是核心：judge 输出 `verdict.enough` 决定走 answer 还是升级
- 引用方案：上下文编号 [n]，LLM 回答标 [n]，后端映射回真实 gid/claim/evidence 生成 cites（LLM 不自由编证据）

**关键调试发现**：
1. bge-m3 加载默认联网访问 HF Hub（即使本地有缓存）→ 加 `HF_HUB_OFFLINE=1` 离线
2. embedding 向量缓存到 `out_views/<stem>.gvec.npy`（CPU encode 一篇几十秒，缓存后秒回）
3. **topK 8→12**：top8 时多相关主张只命中 1 个，judge 看到不全信息会"假够"漏答；top12 显著改善（01059/28433 多题从"升级兜底"变"直接答全"）

**评估（7 篇 × 42 题全过）**：00859/31079/28433/01599/01316/30023/01505——含常规/长文/双栏/数学/恶意论文。零幻觉、全部有据回答、真不足时诚实声明。
- **数学论文（01505）也能答好**（反直觉）：pipeline 阶段 EVID miss 38%（证据回原文差），但语义检索+回答不受影响——"证据核验"与"问答可用性"是两个维度
- judge 是质量闸门：判"假够"=漏答（危险），判"不足"=升级（只多花钱不答错）。质量风险源头在闸门误判，不在第二层召回

### 阶段 L：QA 漏斗重构（L0→L1→L2→L3）+ 176 题全量回归

**背景**：阶段 K 的三层漏斗实际只有"一个 judge 二分 + 升级抠 1-2 个 chunk"。全量测试暴露系统性缺陷，做了一轮架构级重构（详见 QA_FUNNEL_DESIGN.md）。

**D1~D7 七个缺陷与决策**（都在测试中被"逼"出来的）：
1. **L1(overview) 名存实亡** → L0 真正短路：全貌问题不唤醒 embedding
2. **候选集逐层收缩**（judge 只能从 top12 claims 的 chunk 里挑）→ 加 ChunkIndex 全文检索兜底
3. **升级粒度太小不复查** → L2 增量扩展（圆心 ±1→±2→±3，预算账本）
4. **无诚实收尾** → answer_unknown（"我不知道缺 XX"）
5. **单圆心脆弱** → Judge1 输出 target_sections 定向多圆心
6. **Judge 一套模板** → prompt 工厂四层分模板（各层"够"的标准不同）
7. **gap 不驱动取料** → gap + target_sections 作为下一层取料的输入

**架构决策**：
- ❌ 弃用"预分类 router"：额外的推理任务换毫秒级检索，不值；改为证据驱动的逐级下钻
- ❌ 弃用 L2 整节拉取（大章节预算爆炸）→ 增量扩展
- ✅ L3 独立保底：不继承前面证据，用原始问题重做全文检索（错误不传导）

**全量测试（21 篇 × 176 题）暴露并修复的 bug**：
- **S1 最严重：judge 展示 chunk 二次截断 `[:900]`**——答案原文在 chunk 第 1656 字符处被切，29290-C2 等 5 题"该答出却答 unknown"。**教训：省预算只能控块数（topK/圆心/半径），不能砍单块内容**（写进设计文档铁律）
- **S2 同源：QA 取料 `[:3000]` 二次截断**——实测 29.6% chunk 超 3000，尾部被静默丢弃；chunk 分块期已按 ~4000 切好，QA 不该再截
- **S3 LLM 判定不稳定**：01599-C4 同一问题两次跑结果不同（unknown vs L1 正确）

**测试方法论沉淀**：
- 问题集双视角：dev（按档位验收漏斗行为）+ user（真实口语）
- 判定核心是"答对没有"（关键词覆盖），档位只作行为记录不罚分——L0 直答正确是漏斗优点
- 176 题校准后 ✅176/⚠️0/❌0；剩余 38 条 ⚠️ 逐条人工核对全部是**测试标注问题**（must_have 措辞过严/must_not 荒谬/数字格式），非系统错误
- `run_qa_v2.py --save-baseline` 固化基线，之后改代码重跑自动 diff 勾叉变化（与 bench 同理念）

**面试可讲**：全量测试是"逼出架构问题"的放大器——不跑 176 题，900 截断这种 bug 永远发现不了；同时测试集校准和系统修复要分开记账，避免把标注错误当系统 bug 修。

---

## 2.1 阶段 M：QASPER 233 基准评测（v2.0 定稿）

**背景**：21 篇手写题集能校准漏斗行为，但它是"我们出题我们判"，有同源偏差。
引入 **QASPER 第三方基准**（S2ORC NLP 论文 × 人工标准答案 × 异源 GLM/deepseek 裁判
打 1~5 分，≥4 pass）当独立标尺：233 题全量评测 + 逐级优化（详见
`qa/QASPER_EVAL_LOG.md`）。

**V1 基线（评测 bug 修复后）**：118/233 = 50.6%。修复前 100/233（42.9%）
——评测层三个 bug（cites 截断 / judge JSON 解析 / chunk 二次截断）曾造成约 7.7pt 失真。

**优化链（每步都是"定向子集重跑验证后再合入"）**：

| 轮 | 改动 | 回收 |
|---|---|---|
| E1 | 浅层 fail 强制 L3 深潜实验 | 15/40（验证深潜价值有限）|
| V2-A | judge 题型完整性自检（列举/数值/对比须判"全"）| +14/40 |
| V2-B | answer SYSTEM 5 条准则（yes/no 下结论等）| +4/21 |
| V2-C | **judge_l3 放宽**（兜底层别再拒答，允许拼凑/推断）| **+15/38** |
| 两步法 | L2/L3 先穷举 extract facts 再作答（防漏分散成员）| 大量漏项被救回 |
| answer 强化 | 对象裁决 / 专名必写 / 收尾核对 / 防自毁 共 11→13 条 | 19 题 A/B 类验证 2 真翻 0 回归 |

**结果**：`065230` 全量 **176/233 = 75.5%**（较 V1 基线 +24.9pt；较评测 bug 前 +32.6pt）。

**剩余 57 非 pass 归因（关键认知）**：
- 表格/图数据层缺口 ~23（QASPER 是纯文本源，F1/域名/列表在 Table 里，全文根本没有）→ **不可修**
- answer 阅读理解 ~16-18（引用段已含 gold 仍答错：答错对象/枚举漏/机制漏核心）→ 主战场
- 有答案拒答 10（检索没送到 or answer 过度保守）
- 真 gold 空 1、问题歧义 ~3（基准噪音）

**方法论沉淀**：① 评测 bug 要先于系统 bug 修；② judge 判"够"≠ answer 能答对，
漏斗只能把证据送到眼前，answer 层才是质量主战场；③ LLM 有波动，单次翻盘要定向复跑复核。

### 阶段 N：新论文批次外推 QA + 缓存掩盖的 figures 回归（2026-09-06）

**任务**：用 10 篇 2026-09 最新 arXiv（LLM-Agents/对齐/多智能体系统，跨出原 NLP 主分布）做首次外推 QA——每篇新写 10 题 dev/user，全链路 pipeline + QA v2 漏斗。结果与记录见 `qa/QA_V2_NEW10_20260906_REPORT.md`。

**暴露并修复的回归（重要）**：新论文首次无缓存跑 `stage_figures` 崩 `IndexError: no such group`——
commit `0e6848e`（类型收敛）把 `_CAP_HEAD_RE` 两个捕获组误改成非捕获组 `(?:...)`，
代码却仍按 `m.group(1)/group(2)` 取用。此前冒烟/bench 全命中缓存 figures.json，`extract_figures`
从未在新论文上真正执行，缺陷被缓存掩盖两个月。**教训：改"只读产物存在性"的回归检查天然测不到
"首次生成"路径——bench FIG 应加删缓存后重算的用例。**

**QA 成绩**：100 题零崩溃，✅87/⚠️13/❌0（语义修正后 90/100）。13 条 ⚠️ 人工复核分三类：
A 标注措辞过严 3、B 缺细节数值 4、C 真系统缺口 6。C 类中 **4 条是"论文没写"式误断言**
（answer 在浅层断言原文缺数据/原因，3 条回原文逐字核验坐实数字/原因存在），1 条数字答错
（71.8 vs 真值 80.9），1 条 yes/no 缺席判断仍拒答。深潜层 L3 作答 16/16 全对 →
**质量缺口不在检索能力，在 answer 的"缺失断言"缺闸门 + 浅层收口过早**。

**（2026-09-07 已修）缺失断言复核闸门**（`answer.py`，不改图结构）：浅层 L0/L1/L2 答案若带
"论文没写/未给出/找不到"口吻，自动执行 L3 独立全文复核（`ChunkIndex.search_hybrid`
向量+BM25）——可作答则 L3 重答（纠正误断言），全文确无则保留原答案（此时缺失结论才可信）。
定向探针 4/4 救回（02786-06→2.37/0.79、02056-U2→HGT 29.99%、03718-U3→80.9/92.7/96.4、
04170-U2→失败根因）；全量重跑 ✅87→89 / ⚠️13→11 / ❌0，**0 回归**；真实缺口收敛到 2 条
（02056-04 浅层漏最终规则、03335-07 yes/no 缺席拒答）。另在 bench FIG 补"无缓存重算
extract_figures"用例，防 figures 回归再被缓存掩盖。


---


### 阶段 O：QASPER 1000 题整夜扩量 + 收口决策（2026-09-07）

**完整记录见 `qa/CAMPAIGN_20260906-07.md`**（本段只留要点）。

**做了**：QASPER train 全库排除历史已评 242 题 → 全新 1000 题整夜评测（2 worker 并行 + JSONL 断点续跑 + supervisor/看门狗自愈 + 每小时巡检）。期间暴露并修复一个评测基建 bug：`.env` 裁判 key（glm-5.3）无余额 → HTTP 429 全挂 2h；修复为 worker 启动时把裁判镜像到主链路 key，并把日志改为各进程直写（教训：评测基建要自愈+可观测）。

**成绩**：**750/1000 = 75.0%**（0 error），与旧 233 题 75.5% 双样本一致 → 这是当前 flash 管线的真实外部水位，不是小样本虚高。

**残差画像（250）**：① 检索/材料送达 ~45–55%（含 1a 纯召回 / 1b 表格数据层 ~80–110 / 1c 判太严被拒）；② 材料到了没答好 ~35–40%（枚举/对象/绝对化/误断言）；③ 基准噪音 ~5–10%。

**收口决策**：
1. **不再反复调 prompt**（外部口径净收益≈0，LLM 波动即 ±2~3pt）。
2. **② 类是模型能力问题**：A/B 验证 `deepseek-v4-pro` 12 条非 pass 翻 7、对照零回归 → 想要就整链路换 pro（选型，非修 bug；独立乘法轴）。
3. **① 类是唯一剩余工程方向**，优先级：MinerU 换解析层（1b，最大但需先 spike：接口保持 `parse_pdf→blocks`、表格只进检索不进 claims 防伪 claim、授权/体积）＞ 检索策略（1a，A/B 便宜，收益未知）＞ 1c。
4. yes/no 细拆（45 非pass = 真没查到 23 / 方向错 20 / 只差明说 2）：放宽口径最多 +2，不值得；"先开放事实后裁决"是可能的方向但未做。
5. 系统水位以 75%（flash）为正式成绩，边界账本见 CAMPAIGN 记录 §6/§7。
6. （2026-09-07 晚·检索层收尾/定版）L3 改 `search_hybrid`+topK12、unknown 收尾改"提炼原文给用户"；查询改写 A/B（40 题 ON9 vs OFF12）**负收益、默认关**。250 条非pass 用当前代码重跑：**66 翻绿（26%）**，定版估计 ~79–82%（旧 pass 不回退假设），详见 CAMPAIGN §10。

### 阶段 P：定位收尾——从问答得分器到论文帮读器（2026-09-07 晚）

**完整记录见 `qa/CAMPAIGN_20260906-07.md` §11**（本段只留要点）。

**做了什么**：补齐评测矩阵最后四块并重定位产品——① 三列公平对比（直接 LLM/朴素 RAG/本系统，64 常规 + 30 硬题，同题同 glm-4-flash 盲判）；② 防幻觉负向组（篡改/不存在物 20 题）；③ 稳定性同义改写（30×3）；④ 压力/边界（超长文/坏 PDF/对抗诱导）。随后因"漏斗在 pass 率上增益很小、hard extractive 甚至帮倒忙（1/12 vs 直读 6/12）"的诚实结论，把定位从"问答得分器"改为"给小白读论文的帮读器"，并补 A（小白外行问答）+ B（导读数值忠实抽检）两套帮读评测。

**硬数据**：小白问答 15/15；导读数值回原文 102/102（0 缺失）；防幻觉 20/20；对抗诱导 12/12；压力：超长 168K 字符 B2 8/8、坏 PDF 干净报错不崩溃、扫描件不可用（需 OCR/MinerU）；稳定性 3 问法 pass 一致 70%（波动主因=judge 判定随问法变 + 缺失闸门只兜 L0-L2）；对比：常规 51/49/46（增益在拒答与引用，非 pass）、硬题无整体优势（无答案拒答 3/3 vs 1/3 是唯一稳赢）。

**新沉淀的决策/认知**：
1. 模型（deepseek-v4 等）是引擎不是对手；"回忆/摘取"型任务上复杂度不保值，朴素 RAG ≈ 我们；"判断/拒答/溯源/防骗"型任务上复杂度有可测价值。
2. 给小白讲论文责任更大：帮读红线 = 主动产物忠实度（数值维度已证 0 缺失；文字/关系级未全覆盖）。
3. 将来可选（证据驱动，非乱修）：按题型路由让 extractive 绕漏斗直取原文、缺失复核扩展到 L3→unknown、MinerU 表格接入（需 16G 笔记本 spike 先验体量）。
4. 评测基建可复用资产：`cli/run_compare.py` + `qa/compare/_summarize.py`、`qa/negqa/_run_neg.py`、`qa/robust/_run_robust.py`、`qa/reader/_fidelity.py`；`llm.py` 加 usage/调用计数（向后兼容）。

---

### 阶段 Q：L2 存废之争与架构收敛——"用数据推翻又保住 L2，再决定卸掉它"（2026-09-08）

**完整记录见 `qa/RETRIEVAL_EXPLORATION_20260908.md`**（本段只留主线）。

**主线**：对 L0→L3 漏斗做"这层到底值不值"的拷问，逐层用 A/B 验证。
1. **noL2 A/B**：砍 L2 直 L3，Δ=-1（噪声内）——L2 存废危机。（⚠️ 该样本 12/50 因缺 report 双臂 err，绝对率失真，Δ 侥幸不变。）
2. **方法论修正**：L2 的服务对象是"judge_l1 不够但有 target_sections"的中等问题，不是 hard——改在 **L2 目标题群(67/80)** 上测。发现 L2 **减负卖点真实**（normal 上省 38% prompt，双过 34 题里 21 题 L2 层答对），但准度不占优（直 L3 44 > 扩窗 40）。
3. **净亏题诊断**：判够时窗口∩gold evidence 仅 0.01-0.04（L3 同题 0.98-1.00）；圆心命中仅 10% 而 L1 池含 gold 70% → 卡在"每节 score 最高单 claim"圆心选择 × judge_l2 首窗判够 × 单块不够拼全，三因叠加。
4. **修复五连 A/B**（同 67 题）：字符串硬指标 0 触发（QASPER 语义问句无信号源）；strict(严判够+假阳性护栏)=44；multicent(多候选圆心)=40（圆心喂对但单块仍不够拼全 → 单修无效）；**comb(strict+multicent)=45，首次超过直 L3，token 省 10%**。
5. **架构收敛**：comb 也只领先直 L3 1 题（噪声内）→ L2 这层复杂度买到的准度≈0，卸掉。参考主流复杂查询管线（问题维度分解→并行检索→rerank→父块展开/子块锚点→配额→map-reduce），判断其落点是"更强的全局检索+合成"，不是"更强的 L2"。

**硬数据（同 67 题 L2 目标群）**：base(现状)40 / noL2(砍L2)44 / strict 44 / multicent 40 / comb 45 / 直L3 44。诊断：L1 池含 gold 70%、圆心命中 10%、净赢题圆心命中 67%。

**新沉淀的决策/认知**：
1. **L2 的可赢空间已被数据封顶（≈1 题）**——这不是"不够努力"，是这层设计（节内圆心+扩窗）对"跨块拼全型"题（列举/流程/多方法）结构上够不到，而全局检索天然覆盖。
2. **v3 方向（收敛未实现）**：两级 = L0(总览直答) + L3(全局检索主路径)；L1 claims 降级为 L3 内分支（够就直答）；分类器放 L0 后（只分 L0 未解题走哪个分支，分支内降级链兜底，不做硬路由）；多维分解做成 L3 的第三分支。
3. **评测口径资产**：L2 目标题群圈法（judge_l1 不够+target）、窗口∩gold 重叠诊断、圆心质量离线诊断（L1 池/节/圆心三级命中）——v3 复验沿用。
4. 代码全部开关式、默认图未动（`judge_l2_strict`、`PAPERPILOT_L2_MULTI_CENTER`、noL2 图、llm usage），大改前建议冻结 `tag v2-funnel-final`。

---

### 阶段 R：v3 两级定稿 + 组件化方向（2026-09-09）

**完整记录**：`docs/RAG_COMPONENT_NOTES.md`（目标管线与组件默认配置）、
`qa/RETRIEVAL_EVAL_FRAMEWORK.md`（评测 C/T/E/A）、`qa/recall/ab_v3regress_result.json`（定稿证据）。

**做了什么**：
1. v3 纯两级图（L0 总览直答 → 不够 → L3 全局混合检索 → 答/拒）**定稿替换默认**（`graph.ask` →
   `qa_graph_v3`，v2 保留 `ask_v2` 回退）。
2. 定稿三路证据：权威抽样 250 题同裁判 A/B **V0 203 ≥ v2 201**（+2，calls 3.7 vs 5.6）；多方法
   对比题专项 **82%**（数值子类 50% = 记录短板）；中文 QA276 真实回归 **0**（唯一 ⚠️ 为关键词
   同义误报"策略/战略"）。
3. 用数据逐一证否的中间层（**不引入**）：L1 claims 分支（V1 62 < V0 64，捞不回 L2 那 8 题——
   那 8 题实为 L3 检索召回短板）、L2 扩窗（comb 45 只超直 L3 1 题、噪声内）、多维分解分支
   （多方法题 82% 已够，其真正短板是表格数值=MinerU 待办）。
4. 决策转向**大厂模块化 RAG 管线**（DocumentSplitter/Router/Rewriter/Retriever/Reranker/
   ContextBuilder/Generator/Validator），探索结论固化为各组件默认配置。

**硬数据**：v3(两级) 81.2% vs v2(四层漏斗) 80.4%（250 题同裁判）；成本 prompt +17%、calls −34%。

**新沉淀**：
1. **单篇场景检索复杂度不保值**（朴素 RAG≈我们、rewrite 负收益）——组件化真正价值在工程清晰
   与多篇扩展，不在单篇 pass；单篇潜在增量在 Validator 数值校验与 MinerU（表格）。
2. judge/Router 用 LLM 预测危险（L2 曾 61%），必须带降级链；L0 二分可靠因判断对象简单 + 判错不致命。
3. 过渡方式 = `components/` 薄门面命名归位（逻辑后搬），`agents/graph` 保留，不重写不破坏。
4. 本阶段冻结：`git tag`（见提交历史）保存 v3 定稿 + 全部探索档案。

### 阶段 S：MinerU 接入 · 闸门误杀修复 · 2.0 全量评测（2026-09-10 ~ 09-11）

**完整记录**：`qa/REPORT_2.0_20260911.md`（五道评测全量结果 + 口径与局限）；组件细节见
`docs/RAG_COMPONENT_NOTES.md` §5.6 / §5.7 / §6。

**做了什么**：
1. **MinerU 解析后端接入**（`PAPERPILOT_USE_MINERU=1`，产物 `assets/artifacts/out_mineru/<stem>/`；
   同时把 PDF/产物归位到 `assets/papers` / `assets/artifacts`）。
2. **闸门两条误杀修复**（`components/validator.py`，**只放宽、均有界**）：① 原文自带引用豁免——
   越界 `[n]` 若在被引证据原文里原样出现，是**论文参考文献号**被模型照抄（如 `existing research [29]`），
   不是悬空的上下文引用；② 派生数豁免——答案同时给出操作数与结果、且操作数均有据时，差/和
   （`|a−b|`/`a+b`）视为合规推算（**SYSTEM 准则 8 本就要求算出差量**）。
3. **2.0 全量评测**：补齐此前从未测过的两个维度——**检索尺子**（250 题）与 **chunk 向量空间指标**（3,247 块）；
   并全量重跑 MinerU hard / 防幻觉 / QASPER 233 / run_bench。

**硬数据（2.0）**：

| 评测 | 2.0 结果 | 对照 |
|---|---|---|
| MinerU 端到端 hard（10 篇 × 3 题） | **30/30 = 100%**（均分 4.70） | 修复前 26/30（4.13） |
| 防幻觉负向组（20 题） | **20/20**（编造 0 / 误断言 0 / 待人工 0） | 基线 20/20 |
| QASPER 233（外部基准） | **193/233 = 82.8%**｜有答案题 **179/212 = 84.4%**｜无答案题合格 **18/21 = 85.7%**｜剔除 4 题基准噪音 **191/229 = 83.4%**（均分 4.21） | V1 50.6% → V2 75.5%（有答案题 73.6% → 84.4%） |
| 检索尺子 250 题（208 篇） | R@12 **0.984**（混合）；gold 定位 **250/250** | — |
| chunk 向量空间（3,247 块） | 切分干净（重复块 0.03%、边界断句 4.4%）；**排序精度弱**（AUC 0.747、margin>0 28.9%） | 首次测 |
| `run_bench`（33 篇） | **与基线一致，无变化** | — |

**新沉淀的认知**：
1. **切分不是瓶颈**（有反证）：5 种切分策略（现状/512/1024/2048/段落打包）的 AUC **全落在 0.66–0.71**
   → 排序质量对切分粒度**不敏感**；且现状**跨块率最低（0.147）**、等字符预算 24k 下 R=1.000。
   短板在 **retriever 对长科学问句的打分**（全对平均余弦 0.605 = 高基线/各向异性；gold 均 0.479 vs 干扰 0.426）。
2. **输出闸门是当前最大的单点损失**：QASPER 40 道失败里 **10 道（4.3pt）来自闸门兜底**
   （`未能通过内部事实校验`、cites=0）；参照本次 §5.7 的两类误杀经验，其中疑似误杀者应逐条 dump 甄别。
   反面证据：MinerU hard 出现 1 例闸门→修复回路→裁判 5 分（**修复回路首次在真实链路产生净收益**）。
3. **向量缓存必须有显式失效策略**：v2 切分改动使 `cvec` 全量作废，评测会**静默地测旧系统**
   （与阶段 N"figures 回归被缓存掩盖两个月"同类陷阱）。本轮已全量重建（~45s/篇）。
4. **AUC 口径**：必须"**每题各算再平均**"（0.747）；把所有题拼成一个大数组算池化 AUC 会混掉不同题的
   分数尺度，系统性低估可分性（0.642）。
5. **诚实性责任转移**：`unknown_ok` 从 48 → 0，拒答桶消失，守门职责落到输出闸门
   → 负向组从"补充验证"升级为**必须常跑的守门测试**。
6. **无答案题：先是指标虚低，再才是真缺口（2.0 复盘）**：分档看 233 题 ——
   **有答案题 73.6% → 84.4%（+10.8pt）**、**无答案题合格率 95.2% → 85.7%（−9.5pt）**，
   整体 +7.3pt 是这两个反向效果之和。
   - **根因（代码级）**：`graph.ask` 默认走 **`nol3j` 变体**（删 `judge_l3`、一律试答，
     `qa_graph_v3.py:112`），该变体下 **`answer_unknown` 只保留节点、图内无入口**（`qa_graph_v3.py:83`）
     → `unknown_ok ≡ 0` 是**构造性的**。nol3j 换来的 hard +10pt 是真的（ab_nol3j 100 题），
     但它把质量托付给闸门，而**闸门只做事实校验，不覆盖"论文本身没有答案"这一类**。
     代价的形态：答案从"首句明确拒答"变成"先给相关事实、末尾才说没直接给"，裁判由 5 掉到 3（差 1 分）。
   - **口径修正（当天完成）**：这种"给片段让用户自行判断"是**有效交付、体验更好**，不该判低 →
     评测新增 **`excerpt_ok`** 桶（不计 fail）。该桶 **66.7% → 85.7%**，与 V2 的差距由
     −28.5pt 收敛到 **−9.5pt**；剩余 3 题为**真 fail**。判据在 `cli/run_qasper_eval.py`，
     离线重判工具 `qa/recall/_rescore_unans.py`（零 LLM，可对任意历史 run 复算）。
   - **基准噪音**：4 题（3 问题歧义 + 1 真 gold 空，出处 `FULL_RERUN_20260906_REPORT.md` §5 /
     `QASPER_EVAL_LOG.md`）已单列剔除 → 191/229 = 83.4%（V2 同口径 76.9%）；但**剔除只 +0.6pt**
     —— 压低成绩的既不是基准、也不是"拒答退步"，而是**上面那条口径没对齐**。
7. **架构决策：外部知识补答不放在本系统**。论文确实没有答案时，本系统只返回
   "论文未提供该信息 + 相关原文片段"，**不调用模型自身知识补答** —— 否则 `cite` 体系与
   `validator` 闸门（无据数字/引用越界 → HIGH）自相矛盾，**溯源闭环（核心信条）被稀释**。
   将来做成 **MCP 工具**后，由**外层 agent 的 LLM** 负责补答，并由外层告知用户
   "这段不是论文内容、请自行分辨"。**推论**：评测口径必须把"本系统返回不知道"认作
   **正确行为**（`excerpt_ok`/`honest_refuse` 桶 + 负向组常跑）。
8. **输出闸门在数字层是系统性误杀（已逐条坐实）**：QASPER 233 的闸门兜底 10 道，
   触发的 **18 个数字 18/18 全是误杀**（0 例真编数、0 例 citation）。五个根因（详见
   `qa/recall/GATE_NUM_DIAG_20260911.md`）：① `scan_numbers` 单位后缀**吞后一单词首字母**
   （`15,000 most`→1.5e10、`2.7 kernels`→2700、`0.81 between`→8.1e8）；② **LaTeX 科学计数**
   未归一（`$7.30 * 10^{115}$` 被拆成 7.30/10/115）；③ **LaTeX 转义百分号** `54$\%$` 丢失 `%`
   → 裸 54 不够"显著"直接从证据集合消失；④ **年份剥离过宽**（`Switchboard-2000`/`2000 hours`
   的 2000 被当成年份删掉→差量豁免② 找不到操作数）；⑤ 豁免② 只认差/和（除法+四舍五入漏）
   ＋中文数词词界（`百分点` 的"百"被当 ×100）。
   量级：9 道有答案题被误杀成 `score=1` = **3.9pt**（修复上限 82.8%→86.7%）。
   **教训**：闸门是"宁缺毋编"的守门人，但**判据 parser 的鲁棒性就是闸门的误杀率**——
   每次做数值归一化类改动，都要拿"真实论文里的写法"回归（LaTeX 残留、千分位、单位后缀、
   年份歧义），否则守门人会自己制造损失。
9. **闸门数字误杀已修（2026-09-11），设计原则是"封闭语法写死、开放语义不裁决"**：
   改动只在 `numbers.py`（归一化）与 `validator.py`（匹配/豁免），**零结构改动**：
   ① 单位吸附加**两侧词界**（左：数字前是字母则不吸附 → `F1 百分点` 不再造出 100；
   右：单位后紧跟字母则不算单位 → `15,000 most` 不再 ×1e6）；② **LaTeX 语法识别**
   （`$8.5 \times 10^{11}$` / `$7.30 * 10^{115}$` / `54$\%$` —— 属**封闭语法**，一次写死）；
   ③ 「百分」不算量词；④ **年份只在明确语境剥离**（`Switchboard-2000`/`2000 hours` 保留）；
   ⑤ 豁免② 扩到**四则运算 + 5% 四舍五入容差**；⑥ **派生操作数不再被题干数扣掉**。
   **关键决策：单位成员表刻意不扩充** —— 开放词表（`4W`=瓦特还是 4 万）不靠枚举，
   归一化层只规定"长什么样"，歧义交给上层复核裁决（招1）。
   **验证六项全绿**：数字回归集 28/28；闸门 10 题 **10/10 兜底 → 0/10**；
   10 题按真实裁判重跑 **pass 0/10 → 7/10、均分 1.00 → 4.20、翻正 10 变差 0**
   （折算 **+3.0pt**：82.8% → 85.8%）；**红线** MinerU hard 30/30、负向组 20/20；`run_bench` diff 无变化。
   **附带发现**：裁判 glm-4-flash 在**数值逐项对照上会误判**（`mh-30333-q1` 的 DC 四项数字与
   gold 全等仍判"不符"）→ 单题翻车先做 **gold 逐项核对**再归因。
   资产：`qa/recall/_num_regression.py`（回归集，改数值层必跑）、`_gate_probe_qasper.py`、
   `_gate_num_origin.py`、`_gate_fix_verify.py`。
10. **闸门分层补齐：机器只出「信号」，LLM 做「裁决」（招1，2026-09-11）**：
   上面那条修复治的是**可预见**的写法歧义；但**"机器没匹配上 ≠ 论文没有"**这个结构问题还在
   —— 只要有 parser 覆盖不到的新写法（`4W`、三操作数派生 `293×15÷4262`、没见过的 LaTeX 变体）
   就会重现误杀。所以把**最终裁决权从机器交给 LLM**：机器 `validate_numbers` 只产出
   `unverified`（**高召回信号，不是结论**），`gate` 调 `repairer.adjudicate_numbers`
   在真实片段上下文里判"是否支持"——**支持就放行，不支持/复核异常才升 high** 走 repair→兜底。
   这沿用的是 §5.5 已确立的"机器出信号 / LLM 裁决"分层，只是贯彻到最后一个"机器单方面判死"的分支。
   **验证六项全绿**：闸门单测 11/11（含两条新裁决用例）｜数字回归集 28/28｜
   闸门 10 题 **10/10 pass 且裁决零触发**（归一层已消灭误杀 → **招1 零额外成本**，calls 与修复前一致）｜
   🔴负向组 **20/20（编造 0）**｜MinerU hard **29/30、均分 4.77（历史最高）**，`pass 28/repaired 2`｜
   `run_bench` diff 无变化。
   **方法论**：**可预见的问题用规则写死，不可预见的用裁决兜底** —— 两者是防御纵深，
   不是替代；顺序上**先修 parser 再挂裁决**，否则裁决会把 parser 的 bug 掩盖掉。

**遗留**：QASPER **1000 全新题**重跑拿 2.0 无偏水位（233 是**同源调参集**，82.8% 含拟合成分）；
闸门 fallback 逐条诊断；向量缓存签名化；排序精度（各向异性校正/rerank）A/B。

---

### 阶段 T：夜间四项验证——MinerU A/B · 三列重跑 · 233 复跑 · 500 无偏水位（2026-09-11 凌晨）

**完整记录**：`qa/REPORT_NIGHT_20260911.md`（四项结果 + 口径与局限）。

**做了什么**（按用户指定顺序，全程串行，避免 GPU/LLM 争用）：
1. **MinerU vs pymupdf 表格题 A/B**：新增评测开关 `PAPERPILOT_MINERU_INJECT`（默认 `1`，**线上行为不变**），
   在 10 篇真实 PDF × 30 题（其中 12 题金标准在表格）上跑三臂——A0 纯 pymupdf / A1 注入（线上默认）/ A2 全链 MinerU。
2. **三列公平对比 2.0 重跑**：锁 09-07 同题同裁判，就地替换 README 里的过期结论。
3. **QASPER 233 复跑**（复用历史 80 篇样本，同集可比）。
4. **QASPER 500**：从 1000 随机样本切前 176 篇/500 题（无偏、与 233 零重叠，且是 1000 的子集 → 可配对比较）。

**硬数据**：

| 评测 | 结果 | 对照 |
|---|---|---|
| ① MinerU 三臂（30 题；表格子集 12） | A0 30/30（4.67）· A1 30/30（4.70）· A2 30/30（4.67）；表格子集**三臂全 12/12** | 天花板，分辨不出差异 |
| ① 可达性诊断（补做，0 元） | 三臂 gold 数字命中均 **90/90**、**11/11** 题全可达 | 信息本就在 pymupdf 文本里 |
| ② 三列 常规 64 题 | B0 50 / B1 48 / **B2 53**；均分 4.12/4.05/**4.23** | 旧 46/49/51 |
| ② 三列 深水 30 题 | B0 15 / B1 8 / B2 15；均分 3.53/3.20/**3.57** | 旧 10/8/9 |
| ③ QASPER 233（同集） | **202/233 = 86.7%**（有答案 185/212；无答案严格 pass 17/21、**口径合格 20/21**） | 同日 82.8% → **+3.9pt** |
| ④ QASPER 500（配对 500 题） | **421/500 = 84.2%** | V2 365/500 = 73.0% → **+11.2pt** |

**新沉淀的认知**：
1. **MinerU 收益在这套题上量不出来，且原因是机制性的**：三臂齐平不等于"MinerU 没用"——
   可达性诊断证明 **pymupdf 文本层本已含 90/90 金标准数字**，MinerU 注入的是**冗余副本**
   （`2609.01316v1` 实测检索文本 +24% 全是重复表值）。要量化必须换样本（只在表里、pymupdf 抽不出的题）
   或换指标（检索层 top-k 召回），**不能用端到端 pass**。
2. **上游模型在漂移**：B0 的 prompt/模型数日未动却 46→50（深水 10→15）→ **跨时间的对照不可归因于我们的改动**，
   三列只有**同一次运行内**可比。此条对所有历史对照普遍适用。
3. **2.0 无偏外部水位 ≈ 84%**（配对 500 题；V2 同集 73.0%）→ 2.0 真实提升 **约 +11pt**；
   同源 233（86.7%）比无偏高 **2.5pt**，与"233 是调参集、含拟合成分"的判断量级一致。
4. **闸门修复的价值被独立复现**：233 上 +3.9pt，且 **1 分（编造/答非所问）从 10 → 0**。
5. **无答案题并未退化（一度误判，教训要记）**：只看严格 pass 会得到"500 题里 36/52 = 69.2%"，
   像退化；但按**现行口径**（`excerpt_ok`：系统不给结论、但明确说明"论文未提供"并给出带 cites 的相关原文片段
   = 有效交付，合格）实为 **43/52 = 82.7%**；同口径 233 为 **20/21 = 95.2%，与 V2 的 20/21 持平**。
   剩余 10 题（233 1 + 500 9）裁判措辞全是"回答正确/正确指出了 X，但完整性或证据直接性不够"（3 分档），
   **无编造、无答非所问**，且这些题 gold 为空却能被系统找到相关原文 → 疑似 QASPER `unanswerable` 标注噪声。
   → **教训：报"桶化指标"时必须带上全部桶（`pass` / `excerpt_ok` / `honest_refuse` / `unknown_ok`），
   只报 `pass` 会把"已合格但未达 pass 线"的桶误读成退化**（本轮已据此把 2.0 报告 §6.5 的"首要待办"降级）。
6. **失败题归因（补做，`qa/recall/_fail_attrib.py`）**：500/233 两批模式一致——
   **判分基本不是问题**（2/63 = 3%）；   **A 输入通道缺口是大头**（36/63 = 57%）〔口径更正：按 QASPER evidence 类型重算，**真表格类只有 24 题**，
   另 12 题是非 float 的启发式误判 → 通道缺口应记 ≈5.3pt 而非 8.0pt；见 `STILL_FAIL_DIAG_20260911.md`〕，
   证据：QASPER `figures_and_tables` 只给 `file`（`3-Table2-1.png`）+ caption 字符 span，
   **表格数值无文本形式** → 对任何只消费 QASPER 文本的系统**结构上无解**，系统表现是**正确行为**；
   **我们真可改的只有 25 题 / 5.5pt**（闸门残余 4 + 送达 14 + answer 层 7）。
   **剔除不可归因项后：有答案题 388/413 = 93.9%（500）与 185/197 = 93.9%（233）—— 两独立样本收敛到同一点**；
   整体 91.2% / 92.7%。⚠️ 属**能力上限口径**，须与原始口径（84.3% / 86.7%）**并列**，不得单独宣称。
7. **决定：停止单篇反复调优**（用户 2026-09-11）。理由：可改空间仅 5.5pt，且改动互相牵连
   （修送达可能动闸门、改 answer 可能动拒答），返工风险大于收益。
   提水位的方向改为 **① 输入通道扩容**（拉 arXiv 原始 PDF 解析表格，理论最多 +8pt，须单列口径）
   或 **② 多篇 / MCP 工具化**（新场景，不是单篇调参）。
8. **输入通道扩容验证成功，且第一次量化出 MinerU 的收益**（2026-09-11）：
   拉 A 桶 36 题对应论文的 **arXiv 原始 PDF（33 篇）→ MinerU 解析表格 → 追加进检索视图**
   （新增 env `PAPERPILOT_QASPER_TABLES`，**默认关、线上行为不变**）→ 重跑那 36 题：
   **回收 14/36 = 39%，翻红 0**；整体 **84.3% → 87.1%**、有答案题 86.0% → **89.1%**；
   配对 500 题 **V2 73.0% → 87.0%（+14.0pt）**。
   → A 桶的正确定性是「**在 QASPER 提供的输入通道内不可达**」，不是"我们没能力"。
   → **MinerU 的收益第一次有了数字**（在 QASPER 表格这条线；本地区库 A/B 是天花板，信息本就在文本里）。
   ✅ **溯源闭环已逐题验证**（`qa/recall/TBL_CITES_20260911.md`）：14 道翻绿题共 32 条引用，
   **17 条指向表格块 `xtbl-*`、12/14 题至少有一条表格块引用**，validator **14/14 pass**；
   另 2 道引正文块、同样有据；表格引用带真实页码（`#xtbl-51` p3 = Table 3）可跳原文。
   ⚠️ **一次自我纠错**：中途曾把"cites 为空"记为集成缺口 —— 实为**实验脚本 bug**
   （`_qasper_tbl_exp.py` 首版未把 `cites` 写入记录，`.get('cites')` 得 `None` → 全判 0）。
   **教训：报"某字段为空"之前，先确认脚本确实写了该字段。**
   录入工具：`qa/recall/_fetch_arxiv.py`、`_run_mineru_batch.ps1`、`_qasper_tbl_exp.py`、`_verify_tbl_cites.py`；
   报告 `qa/recall/QASPER_TBL_EXP_20260911.md`、`qa/recall/TBL_CITES_20260911.md`。

---

## 3. 当前状态（21 篇基线）

> 注：2026-09-10 起基线已扩到 **33 篇**（`bench/snapshots/20260911-010619.json`，与基线 diff 无变化）；
> 以下 21 篇的描述保留为历史记录。

**全勾 17 篇**：常规论文 + 双栏/奇偶页眉/AAS 双栏+公式+图表。
**带叉 4 篇**（均为已知、有解释的边界）：

| 论文 | 叉 | 原因 |
|---|---|---|
| 31079 | HDR | 3/7 幽灵标题（低危，记录待查，不修）|
| 31166 | CLA + SKEL | 定理-证明型论文：附录证明提取失败 + 定理 hub 结构性孤岛 |
| 01505 | HDR + EVID | 数学群论表：标题污染 + 符号打散导致证据 miss 38% |
| 01555 | EVID | 数学论文：证据 miss 29% |

数学论文的叉**是预期内的能力边界**（当初就是拿来做恶意测试的），不修，保留作记录。

---

## 4. 可以给面试官讲的 5 件事

1. **提取-验真闭环**：每个 claim 强制绑定 evidence 并能规则化回原文，用工程手段对抗 LLM 学术幻觉（不是靠"prompt 让它别编"）
2. **数据驱动决策**：先用 4 种规则去重跑 1934 条 claims 证明"规则无效"，才决定上 LLM 语义去重——每个方案都先用数据验证再投入
3. **可解释打分**：LLM 只分类（8 类 role），分数用可调参的规则公式算（位置分档/量化强度/证据可信度），打分逻辑完全可解释、可复现、可调
4. **回归护栏理念**：bench 用"好论文防回归 + 恶意论文记边界"双用途，本地重算层不调 LLM，改代码后一键 diff 勾叉
5. **"修不修"的决策框架**：幽灵标题（启发式规则、影响局部元数据）选择不修并记录；孤岛（LLM 随机性、影响核心视图）选择低成本修——**不盲目修，也不放任，先判断问题性质**

---

## 5. 遗留与下一步

- [x] 封装层：pipeline.py（一个 PDF → report.json）+ web/app.py（上传→报告→提问）
- [x] 问答层：L0→L1→L2→L3 漏斗 + 176 题全量回归（Run2 ✅176/⚠️0/❌0）
- [x] QA 回归护栏：run_qa_v2.py --save-baseline + diff（qa/baseline.json 已固化）
- [ ] 幽灵标题：保持"已知低危"，若影响问答上下文再处理（在消费层容错，不碰 heading 规则）
- [ ] 孤岛 hub 的产品语义：封装时定处理策略（保留标注/降权/并入）
- [x] 数学/定理证明型论文：**产品决策——不收**（2026-09-04 定：数学论文直接不纳入产品范围，不再投入维护；01505/01555 等仅作 pipeline 边界测试样本保留）
- [ ] 抽样人工复核 QA 答案（✅176 是关键词判定的自动结论，非逐字人工验收）
- [ ] design.md 里的远期：多篇对比 / 主题追踪 / 论文库
- [ ] 类型检查收敛（见技术债）

---

## 6. 已知技术债

- **类型检查（pyright 波浪线）**：当前 Pylance 报大量 `reportArgumentType / reportAny / reportExplicitAny` 等告警，集中在 `cli/run_bench.py` 的 L1 标题收集 dict 用法和 `pipeline.py` 的 dict→pydantic 模型传参处。**策略：现在不动，集中到最后一次性收敛。** 理由：核心接口（pipeline + PaperReport schema + 未来的问答 API）还在演化，提前补类型会反复返工；运行不受影响（Python 动态类型）。接口定型后做一次"dict 流 → TypedDict/模型"的收敛。

## 7. 常用命令速查

```bash
uv run python cli/main.py <pdf>            # 一步：PDF → out_views/<pdf>.report.json
uv run python cli/run_claims.py <pdf>      # 环节：claims + 溯源
uv run python cli/run_view.py <pdf> --force    # 环节：去重/打标/打分
uv run python cli/run_skeleton.py <pdf> --force
uv run python cli/run_figures.py <pdf>
uv run python cli/run_report.py <pdf> --force
uv run python cli/run_scan_noise.py <pdf>  # 噪声扫描
uv run python cli/run_bench.py --save-baseline # 全量勾叉 + 固化基线
uv run python cli/run_bench.py             # 相对基线 diff（改代码后跑）
uv run python cli/run_qa_eval.py [pdf]     # RAG 评估：qa_set 真跑问答图（旧版单层）
uv run python cli/run_qa_v2.py             # 176 题全量（新漏斗）；--pdf/--limit/--save-baseline
uv run python cli/run_bench.py             # report 层回归护栏（勾叉 + baseline diff）
uv run python web/app.py                   # 开发服务器（上传 → 报告 → 提问）
```

---

## 2026-09-13 · 外部代码审查（逐条核验）· 第 1 条：零引用答案"静默放行"

**审查项**：`validator.py:121` 把"答案完全没有 [n] 引用"记为 LOW；`gate()` 只对 HIGH 拦（`:523`）；
`entries` 为空时 LLM 体检不执行（`:445`）→ 实测"完全虚构 + 零引用"的答案 `action=pass`。

**核验**：复现成立，边界钉清 —— 纯文字编造 + 零引用 → `pass`（漏）；**带显著数字**的编造 →
`fallback`（数字层兜住）。根因是 `gate()` 用 `cites` 构造 `entries`，零引用 ⇒ entries 空 ⇒
体检被跳过 + 只剩 LOW ⇒ 只认 HIGH 的 trigger 直接放过。
**真实分布**：503 题 + 72 题零引用共 6 条，**全部是闸门自己的兜底话术**（无一由模型产出），
`level=L0` 的 70 条零引用 0 条 → 防御缺口，非正在漏水。
**附带发现（同一条线）**：`out["validator"]` 的 issues **从未送到前端**（`/api/ask` 不返回、
`index.html` 不渲染）→ 设计里"mid → 前端标注『AI 复核』"从未落地。

**处置（A 档，不改 gate 动作）**：`no_citation` 独立类型；机器判据 `_substantive_claim` 分级
（含实质断言 MID / 纯拒答 LOW）；零引用不再跳过检查而是跑 `_llm_uncited` **格式体检**
（无原文故不判真伪，自带守卫）；`cli/run_qasper_eval.py`、`qa/recall/_qasper_tbl_exp.py`
记录 `validator_action` / `issues` / `no_citation(_substantive)`；`web/app.py` 回传 validator 摘要、
`web/index.html` 渲染"答案自检（AI 复核）"提示条。
**验收**：`qa/recall/_selftest_validator_20260913.py`（机器 12/12；`PP_LLM=1` 加测真实体检）；
`node --check` 前端内联 JS 通过。**未做（B 档）**：按 route 分流强制补引用。
台账：`qa/review/RESPONSES_20260913.md`；设计文档：`docs/RAG_COMPONENT_NOTES.md` §6.5。

---

## 2026-09-13 · 外部代码审查 · 第 2 条：文件名被当作论文身份（同名 pdf 系统性错误）

**审查项**：论文文件名被当成论文身份，缓存会产生错误结果，同名 pdf 会系统性错误。

**核验（`qa/review/_dupname_repro_20260913.py`，已复现）**：全链路产物以 `Path(pdf).stem` 为键，
**没有任何一层记 PDF 内容指纹**。① 流水线：把 A 的 PDF 换成 B 的字节（名字不变）→ **解析层读 B、
产物层给 A 的 claims/报告**（状态混合、零告警）；② 用户可见：`/api/report` 上传"名字=A、内容=B"
→ **返回 A 的旧报告且不写盘**。③ 评测测不到：QASPER 用虚拟名 `qasper_<pid>.qpdf`（身份由数据集给定），
问题只在真实上传路径。

**处置（A + C）**：
- 新增 `src/paperpilot/paper_identity.py`（sha256+size+mtime_ns 指纹；`ok/stale/adopt/unknown` 判定；
  `PAPERPILOT_PDF_ID_STRICT` 开关）；
- `pipeline._pdf_identity_stale` + `process_pdf` 身份门（stale → 重建；`--skip-llm` → **报错拒装配**）、
  `ingest` 入口校验 + 指纹写回、`run_mineru` 校验（同名换内容时 MinerU 也重跑）；
- `document_cache`：`ordered_chunks` / `retrieval_chunks` / `current_source` 的进程内缓存键加入
  **文件版本**（`size:mtime_ns`），保留 `cache_clear` 兼容；
- **历史产物迁移**：无指纹记录时按 mtime 关系收编并补写指纹；全库 66 篇实测 → 32 收编 / 34 无产物
  / **0 被迫重建**（零爆炸半径）；
- **web C 档**：`plan_upload` 按内容判定落点——同名同内容幂等复用；同名不同内容另存
  `<stem>__<sha8>.pdf` 当新论文（原文件不动）+ `upload_note` 告知。

**验收**：`qa/review/_selftest_identity_20260913.py`（19 项全绿）；原复现脚本转为验收脚本：
流水线侧 `[identity] … → 整链重建产物` 且 `--skip-llm` 被拒；web 侧"未返回旧报告 / 已另存新名 /
原文件未改动"。回归：QASPER 虚拟名问答正常、三个自测全绿、`compileall` 0 错。
**未做（B 档）**：产物内容寻址目录（`by_hash/<sha12>/`），成本/收益比不划算。

---

## 2026-09-13 · 摄取异步化：分钟级任务不再挂在 HTTP 请求里（job + worker）

**问题**：`POST /api/report` 原先**同步**跑完整条摄取链（MinerU + 报告链，分钟级）：
用户全程干等、**前端只有一个 await 没有任何进度**、无法取消/重试；而且阻塞调用跑在
`async def` 处理函数里 → **占住事件循环**，同一时间别的请求（别的标签页）全部排队。

**先想清楚"该不该上 Kafka"→ 不该**：Kafka 解决"多生产者/多消费者/高吞吐/可重放/跨服务解耦"，
我们这里是**单机、单消费者、受 GPU+LLM 限速、每分钟最多几篇**，四条一条不沾；
且**Kafka 不会让分钟级变快**（它只搬运事件，瓶颈在 MinerU 推理与 LLM 往返）。
→ 选 `ThreadPoolExecutor` + job 状态落盘；真要跨机时再换 RQ/Celery（接口不变），Kafka 排最后。

**并行的依据（不凭感觉，先核实依赖）**：
`current_source()` 实测 = `pymupdf`（`PAPERPILOT_USE_MINERU` 未设）→ **报告链读 pymupdf**、
MinerU 产物只经 `retrieval_chunks` 注入**检索视图** → 两条路**互不依赖，可真并行**；
一旦 `PAPERPILOT_USE_MINERU=1`（整链同源 MinerU），worker 自动退回串行。
**索引（cvec）必须在 MinerU 之后**（表块要先注入检索视图才能 encode）→ 两条 lane 完成后收口。
并发模型刻意保守：**job 之间串行**（`max_workers=1`，只有一块 GPU，两篇同跑抢显存）、
**job 内部并行**（`max_workers=2`：MinerU 吃 GPU、报告链吃 API 往返，互补）。

**实现**（约 350 行，不动检索/报告算法）：
- 新增 `src/paperpilot/jobs.py`：job 状态**落盘**（`assets/artifacts/out_jobs/<job_id>.json`）、
  取消信号（`threading.Event`）、`recover_interrupted()`（重启后把 `queued/running` 标成中断并可重试）；
- 新增 `src/paperpilot/worker.py`：`_lane_mineru ∥ _lane_report → _lane_index`，
  per-stage 耗时（`on_stage` 回调在阶段边界打点）、协作式取消（阶段边界检查 + MinerU 真 terminate）；
- `pipeline.process_pdf(on_stage=…)`：新增阶段回调（用于计时 + 取消）；
- `ingest.run_mineru(cancel=…)`：`subprocess.run` → **`Popen` + 分段等待**（`run` 无法中途取消）；
  `qa_blocked_reason` 增加 **`running`** 分支（"正在后台解析，请稍候"，与"失败"区分）；
- `web/app.py`：`POST /api/report` → **202 + job_id**；`GET /api/job/{id}`、`GET /api/jobs/latest?pdf=`、
  `POST /api/job/{id}/cancel|retry`、`GET /api/report/{name}`；
- 前端：**轮询**（2s 级）显示阶段与已用时长 + 取消/重试按钮 + `localStorage` 刷新恢复。

**踩到的两个坑（都会让用户"永远转圈"，比报错更糟）**：
1. **Windows `os.replace` 会因"目标正被读"失败（WinError 5）**——CPython 打开文件不带
   `FILE_SHARE_DELETE`，而我们自己的轮询正在读同一个 job 文件 → 一次写失败就足以让状态**永远停在 queued**
   （首轮自测就是这样卡住的）。修：`save()` 重试 12 次（指数退避）+ 非原子直写兜底。
2. **`ThreadPoolExecutor` 会静默吞掉 job 函数抛出的异常** → 加了 `fut.add_done_callback(_guard)`：
   只要 job 还在 `queued/running` 而 future 有异常，就落成 `failed`（带异常名与信息），
   并确保 `_run_job` 的**整段**都在 try 里（连最初的落盘也不例外）。

**验收**（临时冒烟，测完已清理）：
① 模块级：提交 → 轮询 → `ready`；预置取消信号 → `cancelled`；`retry` → 新 job → `ready`；
② **HTTP 端到端**（同进程起 uvicorn，独立端口）：`/` 200、同内容上传**秒回报告**（不排 job）、
`/api/report/{name}` 200、`retry` **202** → 轮询到 `ready`（各阶段耗时齐全）、`cancel` → `cancelled`、
不存在的 job/报告 404。缓存命中的论文 **1.5s 走完**（真实首次仍是分钟级，但现在有进度可看、可取消）。

**代价与约束**：worker 是**单进程内**的（多进程部署会各跑一份，需换 RQ/Celery）；跨进程取消不支持；
索引首次构建仍要 encode（表文本改动会触发 cvec 重建）。
**未做**：SSE 推送（现为轮询，够用）、多 worker、任务优先级/限流。

---

## 2026-09-13 · 工程测试补齐：一条命令 / 离线 / 可上 CI（124 用例，2.8s）

**审查意见**：QA 文件与研究性评测很丰富，但**工程测试不足**。要求：30~50 单元 + 5~10 API 集成 +
1 个 mock LLM 端到端 + 缓存命中/失效 + 越界/零引用/坏 JSON/超长输入 + 一个 GitHub Actions；
**别人一条命令、5 分钟内得到确定结果、不依赖真实 API Key**。

**先纠正一个概念**（否则会白忙）：CI **不需要准备机器**——GitHub 给的是**全新 Ubuntu runner**，
与你本机无关。真正的工作量在**把外部依赖做成替身**，否则别人 clone 下来跑不动。

**做法**：`tests/`（7 个文件）+ `conftest.py` 的 6 个 fixture：

| fixture | 替掉了什么 | 关键点 |
|---|---|---|
| `fake_llm` | LLM 与裁判模型 | 打在最底层 **`llm._chat`** → `chat_json/chat_text/judge_json` 与闸门体检全部自动跟随；按 system prompt 路由到固定响应 |
| `fake_embed` | bge-m3 向量 | **词袋哈希**（MD5 分桶 + 归一化）：词重叠→余弦高，跨平台稳定（不用 `hash()`，避免随机化） |
| `fake_mineru` + `fake_mineru_env` | MinerU（GPU） | 假 `*_content_list.json` + 假装"可用" → 走**真实**复用分支（版本比对/产物校验/meta 落盘） |
| `tiny_pdf` | 真实论文语料 | pymupdf **现场生成**小 PDF（真解析，但无模型） |
| `tmp_assets` | `assets/**` 全部路径 | 14 个模块常量全部 monkeypatch 到 `tmp_path`（**不碰真实论文/产物**） |
| `isolate`（autouse） | 环境与网络 | 清 20 个 env 键 + `urlopen` 换成"一用就炸"（漏替身会**明确失败**，而不是偶发联网成功） |

**覆盖**：124 个用例 = 单元/边界 ~75（表块函数/口径/LLM 解析/闸门）+ 状态机与取消重试 15 +
缓存命中失效 12（含 cvec 指纹、`ordered_chunks` 按文件版本失效）+ API 集成 13 + **mock LLM 端到端 3**
（上传→后台 job→报告→提问→带 `[n]` 引用的答案，其余全走真实代码）。
一条命令：`uv run pytest -q` → **124 passed, 2.8s**（本地；CI 含装依赖约 1~2 分钟）。
需要真模型的用例打 `@pytest.mark.local`，默认 `-m 'not local'` 跳过 → **无 key 也是全绿**。

**迁移而非备份**：把 3 个旧自测（`_selftest_tables` / `_selftest_validator` / `_selftest_identity`）
搬进 pytest 后**删掉旧脚本**——两份断言并存必然漂移（本项目的"27% 被自己匹配器夸大"就是同类教训）。

**踩到的三个坑（都会让 CI 变成"看运气"或"永远红"）**：
1. **`web/app.py` 导入时 `llm._load_dotenv()` 会灌入本机 `.env`（含真 key）** → `is_configured()`
   在**我机器上**是 True、在 CI 上是 False → 同一个测试两处结果不同。修：测试里禁掉该调用 +
   每个用例清 LLM/JUDGE env 键。
2. **用 `spec_from_file_location` 加载 `app.py` 却没注册 `sys.modules`** → pydantic 解析注解失败
   （`PydanticUserError: AskBody is not fully defined`）→ 注册 `sys.modules[name]` + `model_rebuild()`。
3. `pymupdf` **不允许原地覆盖保存**（`save to original must be incremental`）→ 涉及"同名换 PDF"的
   测试改成"写临时文件 + `replace`"（这也更贴近真实的"用户覆盖了文件"）。

**CI**：`.github/workflows/ci.yml` —— `uv sync --dev --no-install-package sentence-transformers`
（`embedder` 里是**懒加载**，测试不导入它 → 连 torch 一起不装，省几分钟）+ `compileall` 语法门禁 +
`uv run pytest -q`。**不设任何 secret**。该 flag 已在本机 uv 0.12.5 用 `--dry-run` 验证。

**分工（不要混）**：CI 判**对错**（离线、确定、免费）；`qa/` 下那批评测判**高低**（真模型、有噪声、
要钱）。"不依赖真实 API Key"这条要求的本质，就是把这两类切开。

**遗留**：`-m local` 目前只有 1 个真实模型用例（闸门体检）——以后可把 QASPER 定向冒烟接进来；
`qa/**/_*.py` 里还有一批复算脚本（口径/分母/覆盖率），它们是**评测工具**、不是单测，继续保持原样。
